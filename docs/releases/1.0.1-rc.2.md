# 晓晓飞行导游 1.0.1-rc.2 候选发布说明

**生成日期：** 2026-08-07（重新打包）

**平台：** Windows x64

**签名：** 无代码签名

**状态：** 候选发布；安装态自动校验通过，等待另一台干净 Windows 机器回归

## 分发文件

给普通用户只发送：

```text
晓晓飞行导游-1.0.1-rc.2-win-x64-setup.exe
```

- 大小：181,738,547 bytes（约 173.32 MiB）
- SHA-256：`c7906d795aa30fcb99e87d1874ea53ef85e1413b7f3e72d6a4dc7a8f16109953`
- 本地产物目录：`release-v2/artifacts/`

`SHA256SUMS.txt` 可以附带用于校验；不要分发 `.blockmap`、`latest.yml`、内部报告或 `win-unpacked/`。由于安装包未签名，Windows SmartScreen 可能显示未知发布者提醒。

## 本次变化

- 采用 V2 最小生产运行时和 ASAR 打包，不再把完整外置 `node_modules` 复制到安装目录。
- 随包固定携带同一次快照中的 `msfs.exe`、`msfsd.exe`、`SimConnect.dll` 和 EFB Bridge，并校验组件 SHA-256 与协议主版本。
- 应用退出和覆盖安装前优先执行 `msfs daemon stop --json`，旧 CLI 不支持时使用兼容兜底。
- 开发机将 Bridge 的“开发版本”和“应用版本”分开保存，MSFS 根目录始终只启用一个版本。
- 全新安装的凭据输入框为空；已保存的 App ID、API Key 和 Access Token 默认显示密码圆点，点击眼睛可查看真实值。
- 探索规划只依赖其实际需要的 DeepSeek 与 MSFS 配置，不再被 STT/TTS 未配置误判阻塞。
- MSFS 主进程的连接监控和探索上下文复用一个 CLI 客户端，默认单通道排队；快照读取改为串行，并对 CLI 超时或 daemon 不可用自动重试一次。
- 打包时从构建环境生成 `out/msfs/geo-config.json`，主进程和 Agent Worker 启动时自动加载，测试者无需手动配置 Geo Cloud。

## 验证记录

- 桌面 TypeScript 类型检查通过。
- 完整 Vitest：52 个测试文件通过、1 个跳过；179 项测试通过、8 项跳过。
- 安装态 LiveKit Agent、RTC、Sharp、OpenTelemetry 和 Electron `utilityProcess` 导入/启动校验通过。
- 安装态 MSFS CLI `status`、`daemon stop`、Bridge 清单与哈希校验通过。
- 安装目录中的 `resources/msfs/geo-config.json` 已确认包含 Geo Cloud 配置字段，未输出密钥内容。
- 安装器已生成：`release-v2/artifacts/晓晓飞行导游-1.0.1-rc.2-win-x64-setup.exe`。
- `rc.1` 已在当前开发机完成安装和主流程人工验证；当前 `rc.2` 仍需在另一台干净 Windows 机器回归。

## 升级与回退

安装前先真正退出晓晓飞行导游，建议同时退出 MSFS 2024。覆盖安装会保留 `%APPDATA%\msfs-ai-voice-guide` 中的用户设置和受 Windows 账户保护的凭据。

回退时先退出应用和 MSFS，再运行保留的旧安装包。如果安装器阻止降级，先卸载当前版本但不要删除用户数据，然后安装旧版。开发机只切换 Bridge 时使用 `pnpm msfs:use:dev` 或 `pnpm msfs:use:app`。

## 已知限制

- 仅提供 Windows x64，无 ARM64/x86。
- 无代码签名和自动更新。
- 尚未在第二台无开发依赖的 Windows 机器完成验证。
- 本候选包明确内置 MSFS Geo Cloud API Key，以支持测试者免配置使用；拿到安装包的人理论上可以提取该 Key。
- `SimConnect.dll` 当前随候选包携带；正式公开分发前仍应按当前 Microsoft Flight Simulator 2024 SDK EULA 或微软书面说明确认该具体二进制的再分发权利。
